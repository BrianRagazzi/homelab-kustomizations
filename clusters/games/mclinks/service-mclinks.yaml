---
apiVersion: v1
kind: Service
metadata:
  annotations:
    # external-dns.alpha.kubernetes.io/hostname: mclinks.games.ragazzilab.com
  labels:
    run: mclinks
  name: mclinks-svc
  namespace: minecraft
spec:
  selector:
    run: mclinks
  type: ClusterIP
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
